{
  "name": "simple",
  "version": "1.0.0",
  "description": "Simple implementation example of @sage/static",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {
    "@sage/static": "workspace:*"
  }
}
